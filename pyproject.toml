[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "pr-analyzer"
version = "0.1.0"
description = "GitHub PR Analysis Tool — Functional Programming Paradigm"
requires-python = ">=3.11"
dependencies = [
    "agno>=1.4",
    "streamlit>=1.33",
    "plotly>=5.20",
    "python-dotenv>=1.0",
    "groq>=0.9",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "pytest-mock>=3.12",
    "hypothesis>=6.100",
    "ruff>=0.4",
    "mypy>=1.9",
    "pre-commit>=3.7",
]

[tool.setuptools.packages.find]
where = ["src"]

# ── Ruff: linting e formatação ──────────────────────────────────────────────
[tool.ruff]
target-version = "py311"
line-length = 88
src = ["src"]

[tool.ruff.lint]
select = [
  "E",    # pycodestyle errors
  "W",    # pycodestyle warnings
  "F",    # pyflakes (variáveis não usadas, imports não usados)
  "I",    # isort (ordem de imports)
  "UP",   # pyupgrade (sintaxe moderna Python 3.11+)
  "B",    # flake8-bugbear (bugs comuns e más práticas)
  "C4",   # flake8-comprehensions (simplificação de comprehensions)
  "C90",  # mccabe (complexidade ciclomática — funções complexas)
  "N",    # pep8-naming (convenções de nomenclatura)
  "SIM",  # flake8-simplify (código desnecessariamente complexo)
  "T20",  # flake8-print (detecta print() esquecido)
  "PT",   # flake8-pytest-style (boas práticas em testes)
  "RUF",  # regras específicas do Ruff
]
ignore = [
  "E501",   # line too long — controlado pelo formatter
  "N803",   # argument name should be lowercase (conflita com lambdas)
  "SIM108", # ternary operator (às vezes reduz legibilidade)
]

[tool.ruff.lint.mccabe]
# Complexidade ciclomática máxima por função.
# Funções acima de 10 são difíceis de testar e entender.
max-complexity = 10

[tool.ruff.lint.per-file-ignores]
# Testes têm regras mais relaxadas (fixtures, asserts longos, etc.)
"tests/**" = ["N802", "N803", "S101", "PT011", "T20"]

# ── Mypy: type checking estrito ─────────────────────────────────────────────
[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
exclude = ["tests/"]

# ── Pytest ──────────────────────────────────────────────────────────────────
[tool.pytest.ini_options]
testpaths = ["tests"]
# --cov-fail-under NÃO está aqui — fica no pre-commit (pre-push)
# para não bloquear execução local de testes durante desenvolvimento
addopts = "--cov=src/pr_analyzer --cov-report=term-missing -q --no-header"
markers = [
    "integration: testes que chamam APIs LLM externas (pular com -m 'not integration')",
    "slow: testes demorados",
]

# ── Coverage ─────────────────────────────────────────────────────────────────
[tool.coverage.run]
source = ["src/pr_analyzer"]
omit = [
    "src/pr_analyzer/ui/*",    # UI Streamlit é testada manualmente
    "src/pr_analyzer/llm/client.py",  # configuração de cliente externo
]
branch = true  # mede cobertura de branches (if/else), não só linhas

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "\\.\\.\\.",  # stubs com ...
]
